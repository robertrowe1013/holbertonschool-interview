#include <stdio.h>
#include "sort.h"

/**
  * heap_sort - heap sort, sift-down
  * @array: array to sort
  * size: size of array
  */

void heap_sort(int *array, size_t size)
{
	if (array == NULL)
		return;
	if (size < 2)
		return;
}
