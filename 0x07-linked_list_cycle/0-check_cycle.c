#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
  * check_cycle - check for cycle
  * @list: list to check
  *
  * Return: 1 if cycle, else 0
  */

int check_cycle(listint_t *list)
{
	if (list == NULL)
	{
		return (0);
	}
	return (0);
}

